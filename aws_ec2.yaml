plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
  # - multiple regions

compose:
  ansible_host: public_ip_address
  os_tag: tags.OS

keyed_groups:
  - key: os_tag
    prefix: os_
  - key: tags.User
    prefix: user_
